---
description: Start the application in dev mode
---

Start the Spring Boot application using `./mvnw spring-boot:run`. Run this in the background. Tell the user the app is available at http://localhost:8080 and remind them they can stop it with Ctrl+C in the terminal.
