---
description: Run all tests and report results
---

Run `./mvnw test` to execute all tests. Report:
- Total tests run, passed, failed, skipped
- For any failures: show the test name, the assertion that failed, and suggest a fix
- If all tests pass, just confirm with a one-liner
