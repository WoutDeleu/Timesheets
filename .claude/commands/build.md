---
description: Build the project and report any compilation errors
---

Run `./mvnw clean compile` to build the project. If the build fails, analyze the errors and fix them. If the build succeeds, report the result briefly.
